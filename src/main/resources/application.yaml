spring:
  application:
    name: flashlearn
  datasource:
    url: jdbc:mysql://localhost:3306/flashlearn?createDatabaseIfNotExist=true
    username: fl123
    password: alwayschange
  jpa:
    show-sql: true
  flyway:
    baseline-on-migrate: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 120 # 10 minutes
    refreshTokenExpiration: 86400 # 1 day
  mail:
    username: ${MAIL_USERNAME}
    password: ${APP_PASSWORD}
gemini:
  apiKey: ${GEMINI_API_KEY}
